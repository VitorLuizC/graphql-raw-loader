const resolver = {
  ' ': '\\s*',
  '<': '(',
  '>': ')',
  '...': '\\.\\.\\.',
  '(': '\\(',
  ')': '\\)'
}

const createExpression = (value) => {
  const source = value.replace()


  const expression = new RegExp('^$')
  return expression
}
